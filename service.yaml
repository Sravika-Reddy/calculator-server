apiVersion: v1
kind: Service
metadata:
  name: calculator-server-service
spec:
  type: NodePort
  selector:
    app: calculator-server
  ports:
    - port: 8080
      targetPort: 8080
      nodePort: 30081